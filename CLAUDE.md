# Family Hub — Claude Code Working Context

Read this first. It tells you what this project is, how it's structured, and the rules of the road.

## What this project is

A self-hosted family calendar + chore management system that runs on a Raspberry Pi 5 with a portable touchscreen, reachable from any device on the home WiFi. No cloud, no subscriptions, no external accounts. It is built on top of an open-source base app (**Skylite-UX**) which is forked and extended with custom features.

Think "DIY Skylight." Full feature/hardware/build detail lives in `docs/`.

## Architecture / stack

- **Base app:** Skylite-UX (**AGPL-3.0**, copyleft — upstream relicensed from MIT) — https://github.com/Wetzel402/Skylite-UX. Fork it; don't rebuild from scratch. Our fork inherits AGPL-3.0: it must stay open-source, network users are owed source (AGPL §13), and a closed-source/proprietary-SaaS version is not an option. The copy-and-run model is fully compatible.
- **Frontend:** Nuxt.js + TailwindCSS
- **Backend:** Node.js
- **Database:** PostgreSQL
- **Deploy:** Docker Compose (`docker compose up -d`)
- **Calendar:** read-only iCal URL pull (Apple/Google). No Apple ID stored.
- **Network:** LAN-only by default; optional Tailscale for remote.
- **Display:** Chromium kiosk mode, fullscreen at boot; cron-driven sleep/wake.

## What Skylite-UX already gives us (don't rebuild)

Calendar with month/week views, iCal sync **and full Google Calendar OAuth** (not just Apple), grocery list ("Shopping Lists"), per-person todos (with recurrence fields `rrule`/`recurringGroupId` already on the model), dark mode + settings panel, Docker Compose deploy. Partial color-coded events. Recipe-app integrations (Mealie/Tandoor) are wired in.

⚠️ **NOT what it sounds like — there is NO authentication.** Upstream has _User Management_ (profiles with name/color/avatar) but **no logins, passwords, sessions, or auth middleware** — `index.vue` itself carries a `// TODO: Authenticate user or route to login page`. Anyone on the LAN currently acts as anyone. **Auth is unbuilt and is a prerequisite for parental controls, per-kid chore check-off, reward approval, and per-person points.** See `docs/features-to-build.md`. (Audited against upstream commit `ffcd435`, 2026-04-02.)

## What we are building on top (the actual work)

Organized by phase in `docs/build-order.md`. Summary:

- **P1 Core:** points + chore tracking, rewards store, recurring chores, family message board, meal planner, dinner schedule
- **P2 Gamification:** streaks, badges, celebration screen, leaderboard
- **P3 Display/automation:** photo screensaver, sleep mode, motion wake, color-coded events, parental controls
- **Settings panel** consolidating all config (see `docs/settings-spec.md`)

## Decisions made (locked)

- **Where it runs:** the _app_ runs on the Pi via Docker. **Development happens on a dev machine (Mac/PC), not on the Pi.** The Pi is headless Pi OS Lite with 4GB shared across Docker + Postgres + Node — fine for running the built stack, sluggish for a live dev loop. Because the whole thing is Docker Compose, it runs identically on a laptop and on the Pi, so we build and test locally and deploy to the Pi with `git pull && docker compose up -d`. **No Pi hardware is needed to start building** — see `docs/dev-workflow.md`.
- **Fork strategy:** vendor Skylite-UX into this repo as a fork we own (single repo, single `docker-compose.yml`). One repo to hand off and one repo another family can clone. The upstream stays a remote so we can pull fixes.
- **Calendar:** Apple iCal is our target (read-only URL pull). Note: upstream _already ships_ a working Google Calendar OAuth integration too, so Google is available for free if wanted. UI lets you add/remove sources generically.
- **Hardware:** not bought yet. Phase 1 is procurement; development proceeds in parallel without it.

## Design principle: build it to be copied

A hard requirement is that another family can clone this and run their own. That shapes every decision:

- **Zero hardcoded family data.** No names, colors, chores, or counts baked into code. Everything seeds from config / the setup wizard and lives in the DB.
- **First-run setup wizard.** A fresh clone boots to a "create your family" flow (admin account, add members, assign colors) — not to our family's data.
- **All config in `.env` + Settings UI.** Secrets, ports, iCal URLs, sync frequency, sleep/wake times — never in source.
- **Clean install docs** so a non-developer can follow `docs/installation.md` end to end.
- Treat "could another family run this with zero code edits?" as the acceptance test for any feature.

## Repo conventions

- Vendor Skylite-UX as our fork; `upstream` remote points at the original for pulling fixes.
- Custom work in feature branches per phase (see `docs/build-order.md`).
- DB schema changes go through migrations, never manual edits — and ship seed-free (no family data in migrations).
- **Authed data fetching:** server-rendered pages must forward the session cookie on SSR — use `useFetch` or `useRequestFetch()`, never raw `$fetch` inside `useAsyncData` (it 401s on SSR → empty data that never refetches client-side). For per-user / time-sensitive data (e.g. the chores board, where "today" must be the client's local date and the container is UTC), fetch client-only (`useAsyncData(..., { server: false })`) and wrap the rendered list in `<ClientOnly>`.
- **Completion-records, not reset flags:** recurring state (chores) is tracked as dated completion rows, so "reset" is implicit each new day — no cron — and history is kept for points/streaks/leaderboard.
- Cron-style recurring logic that _does_ need it (leaderboard reset, sleep/wake) runs inside Docker / on the Pi — document each job.
- Keep everything LAN-safe: never add code that phones home or opens external ports by default.
- **Sessions on the kiosk:** waking from the screensaver clears the session and lands on the profile picker (lock-screen semantics). Members with **no PIN** tap straight in (kid profiles); every ADMIN — and any member a parent gave a PIN — enters it. Don't add a PIN-less path for admins.
- **Parent unlock (elevation):** the kiosk stays signed in as an ADMIN, so role checks alone can't tell a parent from a kid at the shared screen. Management mutations (chore/badge/reward/user/integration CRUD, approvals, point adjustments, school-item CRUD) require a fresh admin PIN: server-side `requireElevatedAdmin` (403 `ELEVATION_REQUIRED` → client retry contract), client-side `useAdminGate().gate(fn)` + the shared PIN modal in `app.vue`. Kid flows (completing own chores/school items, messages, redeeming, own-PIN change) stay ungated. New admin mutations MUST use this pattern.
- **One points pool:** chore completions, school-item completions, and manual adjustments all derive the displayed totals and the rewards balance (`server/utils/points.ts` is the union point). Adjustments affect POINTS ONLY — badges/streaks/completion counts are computed from completion events exclusively (`pointsTotalRaw` feeds badge evaluation, never the net).

## Docs index

- `docs/overview.md` — what you're building, full feature list
- `docs/hardware.md` — parts list, screen + Pi mounting, costs
- `docs/installation.md` — step-by-step Pi setup, Docker, kiosk, sleep cron
- `docs/features-to-build.md` — every custom feature with build approach + effort
- `docs/settings-spec.md` — full settings panel spec
- `docs/network-security.md` — privacy model, network setup, Tailscale
- `docs/build-order.md` — phased build plan (start here for sequencing)
- `docs/dev-workflow.md` — where to develop, how to deploy to the Pi, multi-family packaging
- `docs/source-handoff.md` — original handoff doc, verbatim, as source of truth
- `docs/skylite-ux-review.md` — pre-fork code review + security audit of upstream Skylite-UX (`ffcd435`), with a remediation sequence for the fork
- `docs/pi-hardware.md` — Pi-only sleep/motion-wake add-ons (UNTESTED, requires Pi hardware)
- `docs/deployment.md` — prebuilt-image deploy + Watchtower auto-updates + how to cut a release

## Status

**Feature-complete, rebranded "Family Hub", and published to GHCR.** Built and verified (API + browser, both admin/member personas): first-run setup + PIN-based auth (admin/member roles), calendar, a **Today dashboard** (per-person chores + today's calendar + today's school note), per-person chores + points, gamification (streaks, **admin-editable badges**, celebration, leaderboard), meal planner + auto grocery list (**quantities summed across days**), family message board (post-as-anyone + any-member delete), **rewards store** (admin-editable), a **per-kid weekly school grid** (free-text, own-or-admin edit), a parent-controlled settings panel (roles, PINs, account, screensaver photo upload), and a photo screensaver with QR access. The visible UI was rebranded from the upstream "SkyLite UX" to **"Family Hub"** (the AGPL `LICENSE` + upstream attribution are deliberately kept). Ops: a GHCR multi-arch auto-update pipeline (release tags → image → Watchtower) and a scheduled `pg_dump` backup sidecar with a verified restore. **Latest release: `v2026.6.13`.**

**June 2026 update batch (post-v2026.6.4, unreleased):** QR code now encodes the host LAN address (`APP_PUBLIC_URL` env → interface detection → guidance); todo board horizontal scroll fixed (height chain + visible scrollbar + wheel panning in `globalList.vue`); **parent unlock** (PIN elevation, see conventions) gates all management actions on the shared kiosk; **multi-assignee chores** (join table, each person completes their own copy; data-preserving migration); **manual point adjustments** ("−20 — fighting", points-only, kid-visible reasons); **structured school items** (assignable, dated, check-off like chores, same points pool, overdue carry-forward) alongside the free-text notes grid; **badge upgrades** (ANDed multi-conditions as JSON, per-member appliesTo, editor moved to Settings, tooltips); **interactive dashboard** (check off chores + school items from Today, per-member points/streak chips, celebration). All four migrations rehearsed against a seeded v2026.6.4-schema database AND a fresh `migrate deploy`; full API smoke test passed (setup → elevate → multi-assign → complete → adjust → school → badges → lock).

**Released as `v2026.6.5`** (2026-06-10): multi-arch image published to GHCR, `:latest` moved, and the package is **public** (anonymous pull verified). **`v2026.6.6`** (2026-06-11) ships the Node 22 build, the Dependabot security clearance (24 → 0), and the hot-path perf batch.

**June 2026 batch 2 (post-v2026.6.6, unreleased):** dashboard rebuilt as **per-person columns** (auto-fit grid wraps/stacks when tight; tap a header to collapse; per-person calendar events via the existing event↔user links; family-wide meals/events strip on top); **school "Students only" toggle** (hides ADMIN rows, per-device preference, default on); **grocery-list export** (share icon on any list → plain text copy / .txt download / QR that phones scan); **saved-meals repository** (`SavedMeal` model; sidebar on the planner; drag a card onto a day OR pick from a dropdown in the meal dialog; "Save for later" captures a planned meal); **4 new badge rule types** — `EARLY_BIRD` (≥N tasks before an hour, family-tz via `NUXT_PUBLIC_TZ`), `WEEKEND_COMPLETIONS`, `HIGH_VALUE_COMPLETIONS` (≥N tasks worth ≥X pts), `ROLLING_AVG_POINTS` (avg over last W tasks, requires ≥W) — evaluated from completion events (`getCompletionEvents` now carries `completedAt`); **task timer** (timer icon on chore/school rows → presets/custom countdown overlay, auto-completes + celebration at zero); **screensaver wake = lock screen** (session cleared → profile picker; **kid tap-in**: MEMBERs with no PIN sign in by tapping their avatar, admins always PIN — login API allows PIN-less login ONLY for role=MEMBER with null pinHash); **unread-notes notifications** (`MessageRead` per-user pointer; red badge on the sidebar Family Board tab + dashboard banner; clears on visiting the board; 60s poll + per-navigation refresh). Two migrations: `add_message_reads`, `add_saved_meals`. Verified: 866/866 tests (13 badge-rule cases), lint + type-check green, API-verified live (all 4 login rules, unread lifecycle, saved-meals gating, badge validation 400s, live award of EARLY_BIRD + HIGH_VALUE on completion). Drag-drop/timer/QR client interactions reviewed but not browser-driven. What remains is **not code**: hardware procurement + Pi deployment. ⚠️ The **arm64 image has not been runtime-tested** (no Pi yet). ✅ **Backups verified end-to-end against the prod compose** (2026-06-11): seeded stack → sidecar dump captured all tables + rows (incl. PIN hashes) → `DROP SCHEMA public CASCADE` → restore from the dump → app boots and admin PIN login works. The old ~390 B dumps were the sidecar's **start-time** dump of a not-yet-migrated database — expected on a fresh stack, not a bug; every interval dump after data exists captures everything. Remaining open feature-design items are in `OPEN-QUESTIONS.md`.

**June 2026 batch 3 (branch `feat/areas-history-scheduling`, 7 commits PUSHED to origin, UNMERGED — not in any release):** a six-area feature batch (the "task timer" item was already shipped in batch 2). **Chore Areas** — a configurable `Area` model (emoji/icon + name, managed in Settings) + `Chore.areaId` (onDelete SetNull → "Other"); the board and dashboard group each person's chores by area with Daily/Weekly tags. **Who-marked + History** — `ChoreCompletion.completedById` records who checked a chore off (vs the credited assignee `userId`); a new `/api/history` endpoint + `/history` page unify chore/school completions, decided redemptions, point adjustments, board posts, and badges into a role-scoped activity feed. **Chore scheduling** — `Chore.startDate`/`endDate`/`pausedUntil` (active window + vacation pause), deterministic **rotation** among assignees (daily-per-day / weekly-per-week, on-duty derived in `server/utils/choreSchedule.ts` via `rotationIndex`/`choreActiveOn`; `isAllDoneToday` is rotation/window-aware), **claimable** up-for-grabs chores (`claimable`, first-come, one shared completion across the pool, server 409 on a second claimant), and a per-chore **fixed reward** (`rewardId` → completing queues a pending `Redemption` at pointsCost 0, independent of points). **Stats** — `/api/chores/analytics` (30-day per-user / per-area / least-done) + a `/stats` page with a one-tap admin "Boost +5" on neglected chores (reuses the chore PUT). **Household config** — a `Setting` key/value table + `/api/settings` (GET any member / PUT admin, allowlisted keys) + `useFamilyConfig`, surfacing a configurable **points label** (rename points → Stars/Bucks/$, applied across stats/chores/rewards) and a **grade scale** (with `SchoolItem.grade`). **Documents library** — a `Document` model + a `./files` Docker volume (`FILES_DIR` override, gitignored) + `server/api/documents/*` (admin multipart upload: MIME allowlist images/pdf/txt/office + 25 MB cap + generated `storedName`; member list/download served-by-id with path guards + inline disposition; admin delete row + file) + a `/documents` page. Six migrations (`add_areas`, `add_completed_by`, `add_chore_scheduling`, `add_chore_claimable_reward`, `add_settings_and_grade`, `add_documents`), each verified **zero-drift** on a fresh DB. Verified per phase: type-check, lint, **878 tests**, and a runtime smoke against a live dev server (incl. the first-come 409 and the document upload→download→delete roundtrip). **Deferred (flagged, NOT built):** dashboard configuration (a per-device localStorage toggle, mirrors the existing "students only" pref) and message attachments (board notes auto-expire after 7 days — needs a product call). Also fixed here: `docker-compose-dev.yaml` now reads `${POSTGRES_PASSWORD:-password}`from`.env`(it shares the`postgres-data`volume with the prod compose, which inits creds from`.env`).

**June 2026 batch 4 (branch `feat/dashboard-layout`, 4 commits PUSHED to origin, UNMERGED — not in any release):** the "Architectural & Layout Blueprint" deltas that weren't already shipped in v2026.6.8. **Dashboard layout** — a configurable **pinned weekly note** at the top of the board (`pinnedNoteTitle` / `pinnedNoteBody` Setting keys, edited in Settings → Family preferences); **custom member ordering** (`/api/users` now orders by `[todoOrder, name]`, with per-member up/down controls in Settings wired to the existing reorder endpoint); and a stronger **color guardrail** (each person column uses the member's color as a 2px section border + a ~10% header tint). **Automatic point-boosting** — a neglected recurring chore accrues an escalating, capped bonus on top of its base points, derived from completion history + schedule (no stored state, no cron, no migration): `server/utils/choreBoost.ts` computes a schedule-aware neglect streak (missed due-days since the last completion, floored at the chore's creation day + a 60-day lookback; paused / out-of-window days don't count) → bonus (grace threshold 2, +2 per day, cap 10); the board / dashboard / chores page / stats list show a "+N" badge, completion freezes base+bonus into `ChoreCompletion.points`, and the next occurrence resets to base; admin opt-in via the `autoBoostEnabled` Setting (off by default). **Saved-meal editing** — `server/api/saved-meals/[id].put` + `useSavedMeals.updateSavedMeal` + an edit-mode `savedMealDialog` reached by a pencil button on each planner-sidebar row (create/delete already existed). **Message attachments + shared file storage** — `server/utils/fileStore.ts` unifies documents + attachments behind one MIME allowlist, 25 MB cap, generated `storedName`, and a `resolveStoredPath` traversal guard (the three `documents/*` endpoints refactored onto it, now unit-tested); `Message` gains nullable `attachment{Name,StoredName,Type,Size}` (migration `add_message_attachments`); the board POST accepts multipart (note + file) or JSON (plain note, unchanged), `/api/messages/[id]/attachment` serves bytes inline, delete removes the file, and posting opportunistically prunes expired notes + their orphaned files (no cron) — this resolves batch 3's deferred "needs a product call": an attachment lives as long as its note. The composer gets a paperclip + selected-file chip; notes render an image thumbnail or a download chip. One migration (`add_message_attachments`), verified **zero-drift**. Verified per feature: type-check, lint, **898 tests** (new `choreBoost`, `fileStore`, and saved-meal-PUT cases), and live runtime smokes (auto-boost board ramp 0→+2→+4 + frozen base+boost credit + post-completion reset + toggle; saved-meal create→edit→persist→404; attachment multipart upload→inline download→expiry-prune→404, plain-JSON note still works, documents upload/download/delete roundtrip through the refactored endpoints). **Still deferred:** the dashboard per-device config toggle, and "task updates"/comments (no such feature exists yet — attachments were scoped to board messages).

**June 2026 batch 5 (branch `feat/dashboard-vibrancy`, PUSHED, UNMERGED — not in any release; batch 4 was MERGED to main first via PR #13):** dashboard polish + theming + a History chart. **Dashboard vibrancy + user-first chores** (`app/pages/index.vue`) — the signed-in member's own column floats to the front (a no-op on the shared-admin kiosk); gradient section headers (a sun-badge "Today" header, a warm meal strip with per-slot icons, diagonal member-color column-header gradients) and section-distinct icon accents (chores primary / school green / meals amber), all via `--ui` tokens so themes recolor them. **Family Bulletin** — REPLACES the single settings pinned note with a persistent multi-note board at the dashboard top: a `PinnedNote` model (migration `add_pinned_notes`) + `/api/pinned-notes/*` CRUD (`requireUserSession`, any member adds / edits / deletes — family-trust, global/shared) + `usePinnedNotes` + `dashboard/familyBulletin.vue` (inline composer + per-note edit/delete, touch-friendly always-visible controls). Removed the old `pinnedNoteTitle/Body` Setting keys + the Settings section + the dashboard card. **Per-device theming engine** — a `theme` on `ClientPreferences` applied as `data-theme` on `<html>` (a `useClientPreferences` watcher + the boot plugin, so no flash); `[data-theme="…"]` blocks in `main.css` override the `--ui` tokens for **Birthday** (bright pink + rounded + a canvas-confetti burst on switch), **Minecraft** (stone/grass, zero-radius blocky 3px borders, IBM Plex Mono), **Christmas** (forest green / rich red), and **Halloween** (dark purple + neon orange); selector in Settings → Appearance beside colorMode/font. localStorage only (no server), like dark mode. **History chore-points line chart** — `server/api/chores/points-series` (role-scoped per-day chore points over 30/90d, one series per member; sums frozen `ChoreCompletion.points`) + a hand-rolled SVG `history/pointsLineChart.vue` (cumulative lines, per-member colors, combined-total toggle, hover guide + tooltip, gridlines) — NO charting dependency (Recharts is React-only). **Saved-meal editing** shipped in batch 4 (merged here); saved meals are day-agnostic templates, so "scheduled days" isn't a saved-meal property (planned meals carry day/slot via the meal dialog) — flagged, not built. One migration (`add_pinned_notes`), verified zero-drift. Test harness: `requireUserSession` is now stubbed globally in `test/nuxt/setup-global.ts`. Verified per phase: type-check, lint, **910 tests** (new pinned-notes POST/PUT + points-series cases), and live smokes (bulletin CRUD; theme CSS compiles / login 200; points-series `[5,0,5]` bucketing + role scope). **Still deferred:** the dashboard per-device config toggle, and "default days" on a saved meal. ⚠️ Endpoints that need a server util (e.g. `addDays`) in a unit test must import it explicitly (`../../../utils/choreSchedule`) — vitest doesn't inject Nitro auto-imports; only h3 + auth globals are stubbed.

**June 2026 batch 6 (branch `feat/theme-system-and-icons`, UNMERGED — not in any release):** offline icon hardening + a big theme-system expansion + meals/history/chores polish. **Icon offline-bundling** — an `icon` config in `nuxt.config.ts` (`serverBundle: "local"`, `clientBundle.scan`, `fallbackToApi: false`) so every lucide icon (incl. DB-driven badge/area names, via the same-origin `/api/_nuxt_icon` route) renders with zero external-API dependency on the LAN-only Pi (~83 icons embedded client-side). _Diagnosis:_ current code already renders icons fine — the reported "broken icons" were a **STALE running container** whose DB predated batches 3–5 (verified by driving a headless browser at it); the deployed app needs a rebuild/redeploy. **Theme-system overhaul** (`app/assets/css/main.css`, `app/types/ui.ts`) — fixed the root bug that Nuxt UI's `default` utilities read the UNSUFFIXED `--ui-bg`/`--ui-text`/`--ui-border` while theme blocks only set the `-default`-suffixed tokens (so earlier dark themes never truly darkened): a one-line `:root` bridge makes the unsuffixed tokens follow, and `body` now paints `var(--ui-bg-default)`/`text` so dark themes **and** dark mode recolor the whole page, not just cards. Added **8 themes** (Valentine's, St. Patrick's, Easter, Independence, Thanksgiving, New Year's, Winter, Space), each with a full text-contrast ramp; gave Birthday/Christmas their missing text tokens. **Animated theme decor** — `app/components/theme/themeDecor.vue` (ambient CSS-animated emoji per theme — snow/balloons/leaves/hearts/planets…) mounted in `app.vue`, gated by `prefers-reduced-motion` + a per-device `themeDecorEnabled` toggle; switch-burst generalized to every theme via `canvas-confetti` `shapeFromText` (`app/utils/themeMotifs.ts`). **Auto-seasonal theming** — `app/utils/seasonalTheme.ts` (date→theme windows + Easter `computus`) + a new `"auto"` theme option + a global admin `autoSeasonalTheme` Setting; resolution centralized in `app/composables/useTheme.ts` (an explicit manual theme always overrides). Quick theme switcher added to the dashboard header (Settings selector kept). **History "Back to Everyone"** — a prominent reset button + clickable chart legend (`history.vue`, `pointsLineChart.vue`). **Saved-meal "usual days"** — `SavedMeal.defaultDays Int[]` (weekday multi-select in the dialog; planner shows the days + a one-tap "add to this week"). **Chore "punishment wheel"** — `Chore.wheelEligible Boolean` + an editor toggle + a spin-the-wheel SVG component (`choreWheel.vue`, reveal-only, confetti) opened from a "Spin" button on the chores page; the pool is derived from the already-loaded board (no extra endpoint). One migration (`add_saved_meal_days_and_chore_wheel`), zero-drift on the dev DB. Verified: type-check, lint, **923 tests** (new `weekdays`, `computus`/`seasonalThemeFor`, saved-meal `defaultDays` cases) + live headless-browser smokes (every theme applies & dark themes stay readable; decor renders; icons render incl. the authed dashboard; the wheel renders + **lands on the slice under the pointer**). **Caveat reaffirmed:** a server util used by an endpoint that has a unit test must be imported explicitly (`../../utils/weekdays`) — vitest doesn't inject Nitro auto-imports.

**Released `v2026.6.11` (2026-06-22) — user-first chores on the chores board.** The dedicated `/chores` board now floats the signed-in member's card to the front, matching the dashboard (previously only `/` reordered). The shared float-to-front logic was extracted into `sortMeFirst` in `app/composables/useChores.ts` and is now used by both the chores board and the dashboard (the duplicated inline sort was removed), with unit tests added (**927 tests**). Shipped via PR #16 → `main` → tag `v2026.6.11`; publish CI built and pushed the multi-arch image to GHCR (amd64 + arm64) and moved `:latest`. **Release-tooling note:** the npm `version` lifecycle script (`scripts/version.js`) auto-increments the CalVer micro and also rewrites `ha-app/config.yaml`, so a release bump must keep `package.json`, both `package-lock.json` version fields, and `ha-app/config.yaml` in sync — do NOT run `npm version <explicit>` (it double-bumps: npm sets the arg, then the script bumps again, leaving the lockfile behind). **For the record, the earlier "UNMERGED/UNRELEASED" batches above have all since shipped:** batch 4 + batch 5 as `v2026.6.9`, and batch 6 as `v2026.6.10`.

**Released `v2026.6.12` (2026-06-23) — icons, immersive themes, analytics, event reminders, in-app alerts (PR #17).** A five-phase batch: (A) `areaIconFor()` (`app/utils/areaIcon.ts`) maps chore-area names to Lucide icons as the smart default on the dashboard/board/settings; (B) an immersive theming layer — a fixed `<ThemeBackground>` painting per-theme `--theme-bg-image` (bundled SVG art in `public/themes/` for space/nature/ocean/adventure/fantasy + a readability scrim; gradient glows for holidays), four new aesthetic themes, and theme-aware charts via `--chart-*`; (C) analytics — a theme-aware donut + participation heatmap + missed-chores section backed by a new `GET /api/chores/missed`; (D) event reminders — a `reminders Int[]` column (migration `add_event_reminders`) + a dialog selector (recurrence, local-event editing, and read-only-synced handling were ALREADY implemented — verified, not rebuilt); (E) an in-app notification center — a sidebar bell + popover backed by `GET /api/alerts` (event reminders + reward approvals), gated by the now-functional notifications toggle. Verified: type-check, lint, **947 tests**, fresh-DB migration rehearsal, theme-readability screenshots.

**Notification feature report (deliverable #7):** the "Coming Soon" toggle was a UI-only stub with zero backend — no web-push, no VAPID, no service worker, no PWA manifest, no Notification API. Browser notifications AND Web Push both require a secure context (HTTPS), and Web Push additionally needs internet egress to a push service — neither of which the LAN-only HTTP kiosk has. The feasible, shipped delivery is **in-app alerts via polling** (the `MessageRead` / unread-count pattern generalized into `/api/alerts` + a bell). Future full Web Push would require: HTTPS (e.g. Tailscale or a reverse proxy) then `web-push` + VAPID keys then a service worker + PWA manifest then a `PushSubscription` model with subscribe/send endpoints then trigger wiring. Also deferred (schema-gated): average-completion-time and on-time-vs-late analytics (chores have no due time).

**Released `v2026.6.13` (2026-06-23) — emoji `[]` fix + standalone ChoreWheel + custom theme backgrounds (PR #18).** The user reported `[]` placeholder boxes; ROOT CAUSE = **emoji tofu**: the app used raw emoji as UI (theme-selector labels, `themeMotifs` decor, section headers, emoji-set area icons) and the Pi-kiosk/tablet has NO system color-emoji font, so they rendered as `[]` (Lucide SVG icons were always fine). Fix: (a) bundle **Noto Color Emoji** via `@nuxt/fonts` + append it to the `--app-font-sans` / `SYSTEM_FONT_STACK` font stacks (verified bundled as `@font-face` woff2 in `.output`) so decor + any residual/user emoji render; (b) convert functional indicators to **Lucide** — `THEME_OPTIONS` now carry an `icon` (emoji stripped from labels), the 🏆/⏰ headers became `<UIcon>`, and the area dialog uses a Lucide icon picker (`areaIconFor` still auto-picks). **ChoreWheel** is now self-contained: a family-shared `wheelChores` Setting (JSON) via `useWheelChores` (replaces the board-derived `wheelEligible` pool — the dialog toggle was removed, schema field left unused), with a winner-slice highlight, the "Your Chore Is:" modal (Accept / Spin Again), an optional Web-Audio ding, and an in-wheel Manage panel (add/edit/delete/clear/reset, admin-gated). **Custom theme backgrounds**: per-theme uploaded images (reuse `fileStore`) via `server/api/theme-backgrounds/*` (POST/serve/DELETE, elevated, image-only) + a `themeBackgrounds` Setting (storedName + brightness + blur); `themeBackground.vue` paints the custom image (brightness/blur) over a readability scrim, else the bundled `--theme-bg-image`; managed in `settingsThemeBackgroundManager.vue` (Settings → Appearance). No migration (wheel list + bg metadata live in the `Setting` table). Verified: type-check, lint, **954 tests**, image builds + emoji font bundled + boots.

## Where to pick up

Development now is feature tweaks + deployment, not bootstrapping. Useful entry points:

- **Deploy to a Pi:** `docs/deployment.md` (prebuilt image + Watchtower) or `docs/installation.md`. First flip the GHCR package public so the Pi can pull.
- **Cut a release:** push a `vX.Y.Z` tag → CI builds + publishes the multi-arch image (bump `package.json` + lockfile to match first). See `docs/deployment.md`.
- **Add/adjust a feature:** it will mirror an existing one — chores (`server/api/chores/*` + `app/pages/chores.vue`), rewards, meals, badges, school, message board, dashboard. Follow the repo conventions above (migrations; authed SSR fetch via `useRequestFetch`; completion-records, no cron; client-local dates).
- **Open questions:** `OPEN-QUESTIONS.md`.
